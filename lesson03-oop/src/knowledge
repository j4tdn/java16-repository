1 khái niệm static
A Static cho thuộc tính
+ khii một thuộc tính không phụ thuộc vào bất kỳ đối tượng nào
+ các đối tượng đều có chung một giá trị của thuộc tính này
==> thì cho thuộc tính đó là static

+ khi thuộc tính phụ thuộc/có giá trị riêng cho mỗi đối tượng
==> thì cho thuộc tính đó mà non-static

VD:Car#model

B. Static cho method
