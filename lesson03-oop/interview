1. Có nên/ được phép khai báo hàm khởi tạo là private ko?
+ Helper/ Utility Class
+ Builder

2. Tại sao lại khai báo các thuộc tính trong class là private chứ ko phải public?
+ Đảm bảo quy tắc của OOP, mỗi class chỉ quản lý các thuộc tính của riêng nó
-> # FOP: share data

+ Hạn chế truy cập từ những đối tượng ko cần thiết
(employee & company)

3. Từ khóa this?
+ là đối tượng hiện tại đang gọi đến hàm non-static

4. Từ khóa static -> static attribute & static method & static class