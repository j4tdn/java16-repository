JDBC/JPA-Hibernate

=> Truy vấn --> DML
+ executeUpdate -> INSERT, UPDATE, DELETE --> int - affected rows
+ executeQuery -> SELECT                  --> ResultSet

Statement: Trước khi thực hiện gọi executeQuerry, executeUpdate
   -->   : sql phải đầy đủ và có thể chạy được trong workbench
    		* truyền tham số, + chuỗi vào sql khá khó
    
PreparedStatement:
   --> sql ko cần phải đủ dữ liệu, sẽ được truyền data vào sau thông qua setter và dấu ?
   --> Truyền tham số dễ dàng hơn
   --> Sửa lỗi sql injection

+ sql injection, encrypt password
+ batch update
+ transaction

MVC

getAll()

----- dao -----
interface ItemGroupDao: getAll()
class     JDBCItemGroupDao implements ItemGroupDao

----- service -----
--- được gọi từ View
--- xử lý logic || && gọi đến dao
interface ItemGroupService
class     ItemGroupServiceImpl implements ItemGroupService
