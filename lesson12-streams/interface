lambda là 1 cách viết ngắn gọn để tạo ra một đối tượng cho kiểu dữ liệu functional interface with short
	-> chỉ sử dụng được lambda khi đó là 1 functional interface và có duy nhất một tham số
	

	Traversable one(hỗ trợ duyệt 1 lần duy nhất)